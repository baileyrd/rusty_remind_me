[package]
name = "dbs-connector-youtube"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "YouTube connector for rusty_dbs, mirroring dbs.connectors.youtube in baileyrd/Daily-Backup-System"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
dbs-connector-support = { path = "../dbs-connector-support" }
dbs-core = { path = "../dbs-core" }
serde_json = "1"
